# Simple Commit
1. Run `git status` and `git diff --stat` to see what changed
2. Stage all changes with `git add -A`
3. Write a concise conventional commit message summarizing the changes
4. Run `git commit` with that message
5. Show the commit hash and summary

IMPORTANT: Do NOT enter plan mode. Do NOT write plan files. Do NOT save to memory files. Just commit.
